import React from 'react';
import { AppRoutes, paths } from "../routes";
import { useLocation } from "react-router-dom";

function App() {

    return (
        <AppRoutes/>
    );
}

export default App;
